# --- LDAP（ldapi運用の場合は LDAP_URI/Bind は不要） ---
LDAP_BASE_DN=dc=e-smile,dc=ne,dc=jp
# LDAP_URI=ldapi://%2Fusr%2Flocal%2Fvar%2Frun%2Fldapi
# LDAP_BIND_DN=cn=Admin,dc=e-smile,dc=ne,dc=jp
# LDAP_BIND_PASS=@file:/root/.ldap-pass

# --- PostgreSQL（.pgpass を使うため PGPASSWORD は通常書かない） ---
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=postgres
PGDATABASE=accounting
# PGPASSWORD=@file:/root/.pgpass-ldap-password-only